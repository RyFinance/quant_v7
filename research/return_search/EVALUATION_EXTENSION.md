# Evaluation reporting extension — before evaluation returns were computed

The user prioritizes highest return without overfitting. The original rule selects only candidates below 40% annual volatility and 50% drawdown; those limits were research defaults rather than user requirements. Preserve that selection and its primary unchanged. Also freeze two secondary disclosures from the existing twenty candidates: the highest selection-period net CAGR excluding passive controls, and the highest selection-period net CAGR among fitted ridge/gradient-boosting models. These are selected entirely on data ending 2025-06-30, before any final-period portfolio return is computed. No new strategy, parameter, universe or threshold is introduced. If the same candidate wins both, evaluate it only once. Report the much higher risk alongside return, and do not promote whichever happens to look best afterwards as the preselected primary.
